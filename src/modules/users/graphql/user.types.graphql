type User {
  _id: String!
  username: String
  email: String
  password: String
  name: String
  surname: String
}

type Query {
  user(id: String!): User
}

type Mutation {
  deleteUser(id: String!): User
}